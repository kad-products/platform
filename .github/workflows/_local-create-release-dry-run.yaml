name: Create Release Dry Run

on:
  pull_request:
    branches: [main]

jobs:
  create-release-dry-run:
    uses: ./.github/workflows/create-release-dry-run.yaml
    permissions:
      contents: read
      packages: read
    secrets: inherit
