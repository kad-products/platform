# KAD CI

Utilities, scripts, workflows, and whatever else for CI/CD stuff within KAD product repos.

## Corepack / pnpm version

To setup corepack and add the `packageManager` property to the `package.json` just do this:

```
corepack enable
corepack use pnpm@latest
```